class Node{
    constructor(data, right, left){
        this.data = data;
        this.right = right;
        this.left = left;
    }
}

class Tree{
    constructor(root, buildTree = buildTree()){
    }

    
}

function buildTree(array){

}